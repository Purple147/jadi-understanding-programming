# Rading Standard Library of language we use, making account in stackoverflow, datetime.date.today(), type(), pip install fo libraries, sudo
# ipython, our secret wipen in python is Libraries
from datetime import date
Today = date.today()
print(Today, type(Today))

