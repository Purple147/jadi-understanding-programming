# DataBases by Python, we have a lot of Package of python to MySQL, Connector-Python is good, Reading Documents
# sudo pip install myspl-connector-python, using library is very important, after installing we need to connect that
# for connect to mysql we need import that and after thath use .connect(user="", password="", host="", database="") and close that
# in advance world we havent one way in do one thing, so need to 0 Searching 1 Reading 2 Doing
